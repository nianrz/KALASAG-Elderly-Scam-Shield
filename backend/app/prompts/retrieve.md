You extract retrieval concepts from suspicious messages sent to people in the Philippines.

The message below may be in English, Tagalog, or Taglish. Rewrite it into 3 to 6 short English red-flag concepts that describe what the message is doing — the tactics, not the surface words. The knowledge base is in English, so the concepts must be English even when the message is not.

Examples of good concepts: "claims account suspended", "urgency deadline", "shortened link", "casino deposit bonus", "unexpected prize win", "asks for verification via link".

Return ONLY a JSON array of strings. No other text.

Message type: {message_type}

Message:
{redacted_text}
